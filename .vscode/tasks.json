{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "debug",
            "group": "build",
            "type": "shell",
            "command": "cppmake --type=debug"
        },
        {
            "label": "release",
            "group": "build",
            "type": "shell",
            "command": "cppmake --type=release"
        }
    ]
}