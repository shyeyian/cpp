template < class structure, class... types >
class structure_interface
{
    // It is currently empty, but you can add for_each(), sum(), transform() and other algorithms here.
};

#include "structure_interface.cpp"