namespace detail
{
    template < class type1, class type2, class device >
    class transform_to_key;

    template < class type1, class type2, class device >
    class transform_to_value;
    
    template < class type1, class type2, class device >
    class transform_to_pair;
    
} // namespace detail
